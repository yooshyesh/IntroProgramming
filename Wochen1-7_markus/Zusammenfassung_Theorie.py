#def double (number = 0) #
""" wird als default Nummer verwendet, falls nichts
in den Parameter eingegeben wird """

#print((7/2) == (7//2)) # // wie oft die Zahl reinpasst ohne Dezimalstellen zu erzeugen

# for-loop mit ungenutzter Zählvariable mit _
# bei genutzter Zählvariable definieren zB i

for i in range(8):
    i += 1
    
print(i)
 
# Funktionsdefinition und Execution Code trennen

# Verwendete Variablen als Parameter definieren, Argument

# _ vor Variable bedeutet, andere dürfen sie nicht verändern